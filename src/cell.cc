#include <cell.hh>

namespace cc
{

  extern const dir TL(-1,-1);
  extern const dir TR(-1,+1);
  extern const dir L ( 0,-2);
  extern const dir R ( 0,+2);
  extern const dir BL(+1,-1);
  extern const dir BR(+1,+1);

}
